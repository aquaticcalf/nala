{
  "name": "nala",
  "private": true,
  "workspaces": [
    "ai",
    "desktop",
    "mobile",
    "pages",
    "ui"
  ],
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "fix": "oxfmt -c config/oxfmt.json && oxlint -c config/oxlint.json --fix && tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.28.0",
    "oxlint": "^1.43.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
